
import data from './LibraryList.json'


/*export default (state = data, action) => {
    switch(action.type){
        case 'FETCH':
            return state
    default: return state    
    }
} */

export default () => data